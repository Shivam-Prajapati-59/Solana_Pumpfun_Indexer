use anyhow::{Ok, Result};
use tracing::{error, info, warn};
pub struct FetcherInput {
    signature: String,
}

async fn run_fetcher() -> Result<()> {
    info!("🚀 Starting Transaction Parser...");

    Ok(())
}
